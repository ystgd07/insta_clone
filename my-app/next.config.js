/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental: {
        appDir: true,
    },
    images: {
        domains: ['emilyannebondoc.files.wordpress.com'],
    },
};

module.exports = nextConfig;
